# 项目协作规则

- 请使用中文回复。
- 中小型任务禁止自主开启子代理。

## 分支与 PR

- 新增代码、功能、修复及项目文件修改必须在功能分支上进行，通过 PR 合入 `main`。
- 开始修改前检查当前分支和工作区状态；如果在 `main`，先创建以 `feat-` 为前缀的分支。已有本次任务的功能分支时继续使用该分支。
- 禁止直接在 `main` 上提交或向远端 `main` 推送，除非用户明确授权本次操作例外。
- 用户要求「提交」时，提交到当前任务的功能分支；这不代表授权推送、创建 PR 或合并。
- 用户要求推送或走 PR 流程时，推送功能分支并创建或更新对应 PR。合并 PR 需要用户明确授权。
- 保留用户已有的未提交改动，不擅自覆盖、丢弃或混入无关修改。
