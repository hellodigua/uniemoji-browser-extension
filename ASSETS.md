# 内置素材

本地开发版复用 `dsh-emoji/assets/emoji/deepseek` 的 40 张鲸鱼 PNG，语义映射来自该项目的 `catalog.deepseek.ts` 与 `reaction-emoji.ts`。

原始总览图 SHA-256：`3b87fa433ca1ab058a4dcbc020f7e6d8e6c174a1c3587d649a2020544b67e3be`。

代码许可证不包含这些图片，也不声明鲸鱼形象或二创表情的所有权。源项目记录的公开分发确认针对 dsh-emoji；这里的本地复用不代表扩展已获得独立公开分发授权。未发布扩展商店或公开分发包。

素材不完整时保留原 emoji。该仓库不依赖尚待人工校准的各平台枚举。
